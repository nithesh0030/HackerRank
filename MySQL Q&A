1 Q. Query all columns for all American cities in the CITY table with populations larger than 100000. The CountryCode for America is USA.?
A.
SELECT * FROM CITY
WHERE countrycode = 'USA' AND population > 100000;


2 Q. Query the NAME field for all American cities in the CITY table with populations larger than 120000. The CountryCode for America is USA.?
A.
SELECT NAME FROM CITY
WHERE countrycode = 'USA' AND population > 120000;


3 Q. Query all columns (attributes) for every row in the CITY table.?
A.
SELECT * FROM CITY;


4 Q. Query all columns for a city in CITY with the ID 1661.?
A.
SELECT * FROM CITY
WHERE ID = 1661;


5 Q. Query all attributes of every Japanese city in the CITY table. The COUNTRYCODE for Japan is JPN.?
A.
SELECT * FROM CITY
WHERE COUNTRYCODE = 'JPN';


6 Q. Query the names of all the Japanese cities in the CITY table. The COUNTRYCODE for Japan is JPN.?
A.
SELECT NAME FROM CITY 
WHERE countrycode = 'JPN';


7 Q.
